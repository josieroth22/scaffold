# Session Status - July 4-5, 2026

**UPDATE 12 (July 7, evening): the Jocelyn retro-test — Scaffold vs a 20-year answer key.** Josie's real 2014 profile run through the production prompt + verified data in-session on Max (API run cancelled ~$1; profile scripts/profiles/jocelyn-roth.json; blind test: Vandy/WashU/Robertson/Benacquisto withheld from intake). Result: the plan reproduced her actual strategy (Yale SCEA + early publics + Vandy/WashU merit regular round) and named the Cornelius Vanderbilt Scholarship she won at the school she attended; called Yale's aid-zero. Full writeup + 5 product lessons in docs/jocelyn-retro-test.md (merit-coverage depth → task #11; finalist-weekend collision warning → post-demo prompt idea; soft-preference weighting; Benacquisto freshness). Also July 7: internal emails branded (new-submission + alert, commit 6d66abb), crypto IDs verified live via the cancelled run's ID format.

**UPDATE 11 (July 7): sitewide copy pass shipped (Josie-directed, Mad Men lens).** FAQ "Is this just ChatGPT?" fully rewritten (leads with "the questions you don't know to ask", QuestBridge deadline urgency, positions against both chatbots and $10K counselors; model name-drop and "100+ hours" line cut). Across all pages: form time standardized to 20 minutes (homepage step 1, intake note, Jon draft — was inconsistently 10/15); hero "Your plan in 30 minutes" → "Your plan tonight, not next semester"; How-It-Works header → "Twenty minutes of your time. Twenty pages of strategy."; step 2 model name-drop replaced with counselor-questions framing; CollegeVine no longer named in pricing compare; interests-change FAQ no longer promises the unbuilt regeneration feature; refund FAQ now says reply-to-your-plan-email (was a dead-end "email us" with no address); **generating screen's "Don't close this tab" replaced with "You can close this tab..."** (was contradicting the verified tab-close/resume/email flow on the same screen); plan-page "Plan not found" copy clarified. Kept per Josie: "The information asymmetry is the injustice." Also shipped July 7 (all live-verified on production): three homepage CTAs at conviction moments (post-How-It-Works "Get Your Plan", post-samples "Get a Plan Like This for Your Student", post-FAQ "Start Your Plan" + "Twenty minutes of your time. $50. Tonight." microcopy; mobile-checked, longest button 311px at 375px viewport), footer reordered to "Made in Washington, DC with ❤️ for every student's potential", form time standardized at 20 min per Josie (incl. Jon draft). Also this session: prompt caching + batch API + cheaper-model options and model upgrade playbook added to roadmap; demo exit criteria added as TBD (Josie to decide pre-13th); COGS definition in unit economics; per-session Documentation Discipline rule added to CLAUDE.md.

**UPDATE 10 (July 5, evening): mobile layout pre-screen PASSED + sample-plan audit done.** (1) Rendered index.html and intake.html at iPhone size (375px) via local static preview (.claude/launch.json added, config name "static-src"): hero, sample tabs, FAQ, footer, intake gate, form step 1 (with the new session-length note), and the step-7 priorities ranker all render cleanly — no mobile layout work needed pre-demo. NOT covered (still Josie's quick phone scroll): plan page with real content, true touch-drag, locked-phone behavior during the 30-min build. (2) Sample-plan audit memo at docs/sample-plan-audit-july5.md: all three samples factually clean (style, decimals, every scholarship dollar matches verified data); findings = QuestBridge partner flags missing on 3 real partners (folded into task #11) and Jake's over-budget safeties flagged for Josie's judgment read. (3) Claude's project memory refreshed (custom domain, Resend, Squarespace-forwarding-broken, dead .env.local creds).

**UPDATE 9 (July 5, evening): last autonomous batch (commit 44898c4).** (1) Name-combining bug fixed: intake.html now stores student_name = FIRST name only (was combining first+last while every consumer appends student_last_name itself — root cause of "Imani Washington Washington"; the July 4 samples were unaffected because backend profiles never combined). (2) Submission IDs upgraded to crypto.randomBytes (were timestamp + Math.random; plan URLs are the only access control on family data) — roadmap security-audit item checked off, old IDs still valid. (3) Open Graph + twitter meta added to index/intake/plan (text-only cards; no og:image asset yet) — link previews look right when Josie texts Jon the site. (4) Jon message draft saved at docs/jon-message-draft.md, sending day one at Datacor. Nothing autonomous remains: pending work needs Josie (deep reads, ImprovMX, mobile scroll, sending Jon's message) or is parked post-demo (admin revamp #10, data hygiene #11, server-side orchestration).

**UPDATE 8 (July 5, Sunday): BROWSER E2E TEST DONE — 4 real bugs found and fixed, all systems verified.** Imani Washington run (mr7vk33b5cr6px, submitted via the real form): PASS end-to-end in ~22 min with one T1 fix cycle, server kept writing Tier 1 after the tab closed (core disconnect-survival claim PROVEN), resume drove the entire back half, plan email + new-submission email + reply-to all verified, content read PASSED all three traps (Zell Miller "already qualifies" with GSFC core-GPA nuance; "UGA does not offer a BSN" honesty + no-direct-admit-in-GA truth; Emory "the aid math works" $11,687 vs $15K ceiling). Bugs found by the test, all fixed and deployed: (1) resume crashed at 48% — apiCall was scoped inside submitForm, ReferenceError on every resume ever (fix: resume-scoped twin, commit 3b5d71b); (2) still-building banner invisible — inserted at body top behind the fixed topbar (fix: render inside .hero, 5423e8f); (3) **location.reload() silently failed forever** — `const location = city/state string` at plan.html:2255 shadowed window.location, so reload was a method call on "Atlanta, GA", TypeError swallowed by the poll's catch (fix: window.location.reload + renamed locationLabel, 87f003a; hardened poll: setTimeout chain + focus/visibility recheck + no-store + console breadcrumbs, 7314f3e). Also shipped July 5: still-building banner + auto-reload on plan.html (family clicking the permanent link early), intake session-length note (computer comfortable / phone fine / email either way — REMOVE after mobile test passes), age-grade placeholder clarified, admin shows first+last name, server-side orchestration locked as first post-demo project (before Stripe) in roadmap. Test-status flips used: Jake + Imani flipped regenerating/completed several times (both restored to completed; email dedupe flags prevented duplicate sends). Name quirk RESOLVED (July 5, later): the "Imani Washington Washington" double was OUR bug, not a form-entry error — intake.html combined first+last into student_name, while everything downstream (notification email, admin, generate.js) appends student_last_name itself; backend profiles never combined, which is why the July 4 samples looked right. Fixed: form now stores first name only, matching the profile schema. Imani's stored record keeps the combined name (cosmetic, historical). REMAINING PRE-DEMO: Josie's deep reads (Sofia/Priya/Jake), draft the message to Jon (link + Millie2026 + expectations + heads-up request), ImprovMX for plans@, quick mobile pass.

## WHERE WE LEFT OFF (pick up here — running log, newest update first; older entries below may be superseded)

**UPDATE 7 (July 4, night): BRANDED PLAN EMAIL SHIPPED (task #13 done early).** update-status.js family email now matches the site: cream page / warm-white card, text wordmark (sage S + Georgia serif standing in for Newsreader), sage button, muted disclaimer under an e8ede6 rule; table layout + inline styles, no images (nothing for spam filters to distrust). Test sent via Priya's spare round (mr6hjpw1s7g8j0) and VERIFIED in Gmail — Josie approved the design ("love it"), rendered exactly as built, inbox placement again. Brett (mr6c9wqsdt9spd) is now the LAST un-emailed spare test round. Also: watch-pipeline.js now treats completed_with_issues as terminal (was going to poll 45 min on it); self-review of today's diff found nothing else. Roadmap note: the branded treatment can later be applied to the new-submission + alert emails (internal, low priority).

**UPDATE 6b (July 4, night): lowercase-months mystery solved (Josie caught it).** The stored plans were always capitalized; plan.html's table-badge code lowercases cell text for matching and rebuilt the round-note "(Nov 1)" from the lowercased string — every plan rendered "(nov 1)" regardless of data. Fixed (rawText for display, lowercase for matching only, commit 4232a6d) + a markdown-level month-capitalization backstop added to the renderer (eb8c17a). Verified fixed on live Sofia. Lesson for future render bugs: when display text looks wrong but stored data is right, check the table-enhancement pass in plan.html that rebuilds innerHTML.

**UPDATE 6 (July 4, night): Pipeline v2 small items SHIPPED + rising-senior profile drafted.** (1) Regen fast-path: intake.html now reads `validator.flags` from the review response and skips the doomed fix loop on regen-severity flags (run-pipeline.js already had it). (2) Honest completion: new status `completed_with_issues` when the final review fails but the plan ships — set by intake main path, intake resume path, and run-pipeline.js; update-status.js accepts it, still sends the family their plan email, AND sends Josie an alert email (deduped via `issue_alert_sent`); admin shows "Done (review failed)"; resume/checkResume treat it as done. UNTESTED until a run actually fails final review — behavior is additive, normal completed path unchanged. (3) NEW PROFILE for the browser E2E test: Imani Washington (scripts/profiles/imani-washington.json + docs/test-profiles.md) — 12th grade rising senior (untested age band), Atlanta GA $72K (the never-run Georgia HOPE/Zell Miller test), direct-admit nursing, Emory-aid-math question. AWAITING JOSIE'S GO-AHEAD to run (standing rule: no runs without approval). Docs consistency pass also done this session (README rewritten from pre-backend era, CLAUDE.md priorities/test results/key files current, project-plan checkboxes true).

**UPDATE 5 (July 4, night): Email FULLY VERIFIED; reply-routing has one open item.** Sofia email delivered with final copy (subject "NAME's Scaffold College Strategy is ready", "Scaffold plan" hyperlinked, commit 638d806); Jake re-POST sent nothing → once-per-plan dedupe PROVEN. Priya (mr6hjpw1s7g8j0) and Brett (mr6c9wqsdt9spd) are still un-emailed = two spare live-test rounds. Docs updated (CLAUDE.md "Email (Resend)" section, project-plan roadmap checkboxes). Resend click tracking confirmed OFF by default (opt-in now); TLS stays Opportunistic. **OPEN ITEM — plans@ reply routing:** Josie wants replies to show plans@scaffoldcollegestrategy.com, not her gmail. Squarespace email forwarding (plans@ → josieroth22@gmail.com) FAILED with generic "could not be saved" error, retried + incognito, backend-side (domain is a Google Domains migrant on legacy googledomains.com nameservers, likely trips Squarespace's forwarding). NEXT STEP: set up ImprovMX free tier instead (signup at improvmx.com, add alias plans@ → gmail, add 2 MX records @ root: mx1.improvmx.com prio 10, mx2.improvmx.com prio 20 — root has no MX today so no conflict; Resend sending untouched, it lives on the send subdomain). AFTER forwarding verified: remove reply_to line from update-status.js (it currently exposes josieroth22@gmail.com only if a recipient hits Reply — acceptable fallback, works today, fine for Jon demo even if ImprovMX never happens). Optional later: Gmail "Send mail as" plans@ via Resend SMTP (smtp.resend.com, user "resend", password = API key) so outbound replies also show plans@. Also optional: delete unused "Onboarding" Resend API key.

**UPDATE 4 (July 4, night): PLAN EMAIL LIVE END-TO-END.** Resend domain scaffoldcollegestrategy.com Verified (DKIM + SPF + MX at Squarespace, ~13 min propagation; DMARC `v=DMARC1; p=none;` also added). RESEND_API_KEY in Vercel (Production+Preview, Sensitive; key named scaffold-vercel in Resend; unused auto-created "Onboarding" key can be deleted). Test PASSED: Jake completion email delivered to josieroth22@gmail.com. reply_to added to update-status.js → replies route to Josie's gmail (commit 4069796). Idempotency re-fire done (second POST must NOT send again — Josie confirming). Intake email hint updated to mention the emailed link. Admin submissions API does NOT return plan_email_sent (hand-picked summary fields), so don't use it to check the flag; absence there means nothing. Still to verify: generate.js new-submission notification (fires on next real submission), inbox-vs-spam placement.

**UPDATE 3 (July 4, night):** Simulation renamed "Admissions and Financial Simulation" everywhere. Coming-soon gate live and verified (2 signups incl. Josie; view in admin dashboard "Interest list" section or /api/notify-signup?code=<admin code>). **Plan-ready email BUILT into update-status.js** (fires once per plan on completion, no-op until RESEND_API_KEY exists). **WAITING ON JOSIE: Resend setup** — resend.com account → verify domain scaffoldcollegestrategy.com (SPF/DKIM at DNS host) → API key → Vercel env var RESEND_API_KEY (Production) → then redeploy, test by POSTing update-status completed on Jake (mr6ij7f02ymbzw; plan_email_sent not set → emails josieroth22), then flip intake email hint to "We'll email your plan link when it's ready". Same key activates generate.js new-submission notification to Josie. NOTE: .env.local still has DEAD June Redis creds (new DB creds only in Vercel) — local scripts can't reach prod Redis.

**UPDATE 2 (July 4, late): Tab-close protection SHIPPED** — generating screen now shows the plan's permanent URL immediately (bookmark guidance), and intake.html resumes a stalled pipeline on revisit (localStorage `scaffold_active_submission`, 48h window, simplified one-repair-pass ladder, waits out in-flight server generations). Also shipped: coming-soon gate replaces dead $50 button (email capture → /api/notify-signup → Redis set `notify_emails`; access-code path unchanged; see project-plan Stripe bullet for the un-do checklist), footer love line, sample income "/year" clarification, www redirect verified (valid cert, 308), Tier 2 progress bar calibrated (34K), unit economics measured ($6-9/plan band). **NEXT: the browser end-to-end test is now essential** — it verifies the form, the new gate, the permanent-link display, AND resume (submit, close tab mid-build on purpose, reopen intake.html, confirm it resumes and finishes). Use a rising-senior profile to also cover the untested 12th-grade band (draft offer pending with Josie). Jon's-daughter-specific dry run dropped as unrealistic; profile coverage + browser-path test replace it.

**UPDATE (July 4 evening): everything below through item 5 is DONE.** Jake PASSED (mr6ij7f02ymbzw) and all three samples are live on the homepage with working tabs, nav dropdown, and plan-page sample bar. Copy pass done for index.html AND intake.html (honest generating-time expectations, email hint fixed, stale static samples deleted, live sample links in intake dropdown). UptimeRobot monitoring live on /api/keep-alive. www.scaffoldcollegestrategy.com added in Vercel: valid cert, 308 redirect verified. Tier 2 progress bar calibrated (34K chars). Unit economics updated with measured July 4 band (~$6-9/plan). REMAINING BEFORE JON: (1) Josie's deep reads of the three sample plans, (2) dry-run profile for Jon's daughter (needs intel), (3) optional post-demo: apex DNS record update to 216.150.1.1 (Vercel recommendation, old IP still works). Original notes follow:

**Jake's sample run (`mr6ij7f02ymbzw`) was in final review at pause time** — check `api/submission?code=...&id=mr6ij7f02ymbzw` or admin. His T1 passed after one fix (budget_alignment); if the final review passed, he needs Josie's read/blessing, then restore his three TODO-marked slots (search repo for `JAKE_ID`): index.html nav dropdown + sample tab + sample-desc CTA link, and plan.html sample-bar entry + `sampleIds` array.

**July 4 results: THE RE-BASELINE IS DONE AND THE SAMPLES ARE (nearly) REGENERATED.**
- Brett: PASS (mr6c9wqsdt9spd) after the validator/data bug chain was fixed
- Sofia: PASS end-to-end in one submission (mr6eqht6rzc0tj) — now the live homepage sample (replaced Alejandra persona)
- Priya: PASS, cleanest run ever, zero T1 fixes (mr6hjpw1s7g8j0) — blessed, live on homepage
- Jake: in flight at pause (see above). Fix pattern across samples: 3 fixes / 0 fixes / 1 fix, zero regens.

**Remaining before Jon (deadline July 13):**
1. Jake blessing + restore his 3 sample slots (JAKE_ID markers)
2. Copy pass: intake.html form steps still unread (index.html copy reviewed July 4: good; two launch-checklist flags: refund FAQ has no support email yet, "generate an updated plan" implies unbuilt regeneration feature)
3. UptimeRobot on /api/keep-alive (Josie, 5 min)
4. Dry-run profile resembling Jon's daughter (needs whatever Josie can learn)
5. www.scaffoldcollegestrategy.com doesn't resolve — add www redirect in Vercel → Domains
6. Update unit economics + progress-bar Tier 2 expectedChars with measured data (T1 ~30K chars, T2 ~31-40K chars observed)

## RENDER/UX FIXES SHIPPED JULY 4 (afternoon)
- Simulation section relocated after Probability table (was buried below Tier 2); width fix for nested context; sidebar nav entry follows suit
- Nested-list rendering fixed (indented bullets became orphaned bare text — the vanished "Admitted:" branch on Priya's plan)
- Reading guide box on every plan + orientation line on homepage samples section
- No-cache headers on HTML (ends the stale-browser confusion permanently)
- Sample bar on plan pages rewired to live IDs (Sofia/Priya)
- Homepage: Sofia replaces Alejandra everywhere; Priya restored with live ID
- Calibration: admit-probability rules made selectivity-dependent + in-state exception; verified-data-wins grounding in generator AND reviewer; fixer precedence rule; validator auto-fixes sticker costs; word-boundary school matching; validator reads directory index (school-list.json is missing 29 files — task #11)
- Hopkins ED question (Josie): plan's reasoning validated as counselor-grade; ED-for-full-pay-family logic is correct and conditional

## COMPLETED JULY 2

**Platform:** Vercel upgraded to Pro. The 300s function-kill saga's root cause was the dashboard **"Default Max Duration" field** (project Settings → Functions → Advanced Settings) stuck at 300 — it overrides vercel.json. Now 800, verified by probe (survived 390s). vercel.json glob also corrected to `api/*.js` (deployment-root relative), but the dashboard field is the boss.

**Bugs found by live runs and fixed (all deployed):**
1. Prompt contradiction: "stop after the Monte Carlo Parameter Table" made Fable omit the JSON block → output tail now explicit (table → JSON → Data Sources → stop)
2. Second reviewer blind spot: priorities/priority_other missing from FAMILY DETAILS → reviewer called the family's own words fabricated
3. fix-plan token starvation: 4K max_tokens → all-day no-text-block 500s → now 16K + adaptive thinking
4. reconcile-costs: now streams internally, 32K cap
5. `content[0].text` landmines in fix-plan + reconcile (thinking-safe extraction + loud failure with stop_reason)
6. findSchoolSlug matched wrong campus ("Purdue University" → Fort Wayne) → prefers main campus, then shortest name
7. Reviewer grounding: must compare against numbers PRINTED in verified data, never model memory (Fable cited its own Purdue figure as "verified")
8. Admit-probability calibration made selectivity-dependent (+5 cap only under ~40% overall; 85-95%+ at true safeties) **plus in-state public exception** (up to 1.5-2x overall, max 60%, reasoning stated in narrative) — Josie caught both calibration flaws reading the plan
9. plan.html activities-list mangling (Feb bug): label styler matched lowercase prose → now capitalized-only, single labels must start the element
10. **Validator now auto-fixes JSON sticker_cost** to the verified figure (same string surgery as decimals) with a review-severity flag for narrative verification
11. **fix-plan was blind to the JSON block by design** (stripped before the model saw it — three doomed fix cycles) → fixer now sees the full document and may patch JSON values
12. Formatting: months capitalized, ages as "16 years old" (prompt rules + plan.html render fix)

**Tooling:** `scripts/run-pipeline.js` (backend submission driver replicating intake.html's orchestration: stall watchdog, network retry, error-body logging) + `scripts/profiles/{brett-roth,sofia-martinez}.json`. Progress bar: thinking-pause status messages, expectedChars 13K→30K.

**Run ledger (July 2):** run 3 `mr3kolaxqdolm9` (killed by 300s cap, cancelled) · run 4 `mr3wcxhgdzk3o8` (COMPLETE 72K-char doc + sim; review_failed on Purdue bug-chain; kept as QA artifact) · run 5 `mr3xz0p0d2x2x5` (parked at `regenerated`, see top). ~$25-30 API spend today, all of it buying 12 permanent fixes.

**Roadmap additions:** Pipeline v2 gained severity-aware escalation ladder + Tier 2 re-run rung.

## SESSION LEARNINGS WORTH REMEMBERING

- Fable's world knowledge fights injected data: it wrote its remembered Purdue cost into the JSON even with verified data in the prompt, then cited the same number as "verified" while reviewing. Grounding instructions + validator auto-fix are the countermeasures.
- Every failure so far has been infrastructure/plumbing; plan content quality has been consistently strong (run 4's review praised strategy, state aid, REA handling, tier consistency).
- The Feb baseline was 5 attempts to pass on old Opus; the target is beating that on the repaired stack.

## CARRIED OVER (updated)

### Plan rendering
- ~~Activities list wall of text~~ **FIXED July 2**
- EA (merit deadline) badge: fix deployed Feb, still unverified
- Admit rates as decimals in rendered plan: check formatSchoolCompact()/formatSchoolRadar() — unverified
- Honors programs incomplete: GSU/UNC show N/A (~108-school coverage)

### Minor cleanup (low priority)
- Missing CDS schools (US News 80+): Binghamton, Colorado School of Mines, Chapman, Creighton, Elon, Saint Louis, Temple, U of Missouri, BYU, U of Tennessee, Yeshiva
- DACA/undocumented aid, foster care waivers, military/veteran benefits, Native American tuition waivers for state aid doc

## USEFUL

- Live site: scaffold-hazel.vercel.app (+ scaffoldcollegestrategy.com assigned) | Admin: /admin.html (code in src/api/submissions.js)
- Backend runs: `node scripts/run-pipeline.js <brett|sofia|profile.json>` — no form needed
- Watcher: `node scripts/watch-pipeline.js` | Validator test: `node scripts/test-validate-plan.js`
- Duration probe (temporary): /api/duration-probe?seconds=N
